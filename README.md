# Proyecto Final - Sistema de Gestión de Biblioteca

Sistema CRUD de gestión de biblioteca con MySQL, backend y frontend. Proyecto final del curso.

## Descripción

Proyecto desarrollado con NestJS (backend), MySQL (base de datos) y frontend, que incluye operaciones CRUD completas y consultas con relaciones entre tablas: `autores`, `categorias`, `libros`, `usuarios` y `prestamos`.

## Instalación del Backend

```bash
$ npm install
```

## Ejecución del Backend

```bash
# desarrollo
$ npm run start

# modo watch
$ npm run start:dev

# producción
$ npm run start:prod
```

## Pruebas

```bash
# unit tests
$ npm run test

# e2e tests
$ npm run test:e2e

# test coverage
$ npm run test:cov
```

## Base de Datos

- Motor: MySQL
- Tablas: `autores`, `categorias`, `libros`, `usuarios`, `prestamos`
- Scripts de creación e inserción: *(pendiente agregar ruta del archivo)*

## Frontend

*(pendiente)*

## Postman/Bruno

*(pendiente agregar colección de solicitudes HTTP)*